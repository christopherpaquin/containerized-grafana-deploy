apiVersion: 1

# Grafana plugin provisioning
# Plugins are installed via GF_INSTALL_PLUGINS environment variable
# This file documents which plugins are expected

# plugins:
#   - name: alexanderzobnin-zabbix-app
#     version: latest  # Installed via environment variable in container

# Note: The Zabbix plugin is installed automatically via:
# GF_INSTALL_PLUGINS=alexanderzobnin-zabbix-app
# in the Quadlet configuration file
